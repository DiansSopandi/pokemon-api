{
  "name": "pokemon-api",
  "version": "1.0.0",
  "description": "A Node.js API to scrape Pokémon data and store in MongoDB",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist && mkdir dist && cp -r src/* dist/",
    "start": "npm run build && node dist/index.js",
    "dev": "concurrently \"nodemon src/index.js\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.9",
    "cheerio": "^1.0.0",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "mongoose": "^8.9.6"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "nodemon": "^3.1.9",
    "rimraf": "^6.0.1"
  }
}
